name : simple_cicd

on:
    push:
        branches:
            - main

jobs:
    checkout-code:
        runs-on: ubuntu-Latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: run
          run: |
              ls
              whoami
              
           

